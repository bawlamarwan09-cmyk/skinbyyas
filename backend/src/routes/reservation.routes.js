import{Router}from"express";import{createReservation}from"../controllers/reservation.controller.js";import{validate}from"../middleware/validate.middleware.js";import{reservationLimiter}from"../middleware/rate-limit.middleware.js";import{reservationCreateSchema}from"../validators/reservation.validator.js";import{asyncHandler}from"../utils/api.js";export const reservationRouter=Router();reservationRouter.post("/",reservationLimiter,validate(reservationCreateSchema),asyncHandler(createReservation));
