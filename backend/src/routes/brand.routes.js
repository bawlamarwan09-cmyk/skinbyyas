import{Router}from"express";import{getBrand,listBrands}from"../controllers/brand.controller.js";import{validate}from"../middleware/validate.middleware.js";import{paramsSlug}from"../validators/common.js";import{asyncHandler}from"../utils/api.js";export const brandRouter=Router();brandRouter.get("/",asyncHandler(listBrands));brandRouter.get("/:slug",validate(paramsSlug),asyncHandler(getBrand));
