import {Router}from"express";import{getProduct,listProducts}from"../controllers/product.controller.js";import{validate}from"../middleware/validate.middleware.js";import{productListSchema}from"../validators/catalog.validator.js";import{paramsSlug}from"../validators/common.js";import{asyncHandler}from"../utils/api.js";export const productRouter=Router();productRouter.get("/",validate(productListSchema),asyncHandler(listProducts));productRouter.get("/:slug",validate(paramsSlug),asyncHandler(getProduct));
