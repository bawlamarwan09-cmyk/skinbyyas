import{Router}from"express";import{getCategory,listCategories}from"../controllers/category.controller.js";import{validate}from"../middleware/validate.middleware.js";import{paramsSlug}from"../validators/common.js";import{asyncHandler}from"../utils/api.js";export const categoryRouter=Router();categoryRouter.get("/",asyncHandler(listCategories));categoryRouter.get("/:slug",validate(paramsSlug),asyncHandler(getCategory));
