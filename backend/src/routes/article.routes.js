import{Router}from"express";import{getArticle,listArticles}from"../controllers/article.controller.js";import{validate}from"../middleware/validate.middleware.js";import{articleListSchema}from"../validators/article.validator.js";import{paramsSlug}from"../validators/common.js";import{asyncHandler}from"../utils/api.js";export const articleRouter=Router();articleRouter.get("/",validate(articleListSchema),asyncHandler(listArticles));articleRouter.get("/:slug",validate(paramsSlug),asyncHandler(getArticle));
